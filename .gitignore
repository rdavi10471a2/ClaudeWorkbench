bin/
obj/
.vs/
*.user
runtime/
runtime-child/
node_modules/

# host runtime state (index / working / staging)
/runtime/

# monitor-general agent tool policy (mutable user state)
agent-settings.json

# monitor watched-solution config (mutable; written by the workspace picker). Seed from appsettings.template.json.
**/config/appsettings.json
