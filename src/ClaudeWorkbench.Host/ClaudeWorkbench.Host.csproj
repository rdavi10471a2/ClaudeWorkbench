<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>ClaudeWorkbench.Host</RootNamespace>
    <!-- Host runtime state (working/staged watched-solution mirrors) lands under
         runtime/ inside this project. Those are copies of watched-solution source
         and must never be globbed into the Host assembly, or duplicate-type
         (CS0101/CS0111) build errors appear once a watched solution is configured. -->
    <DefaultItemExcludes>$(DefaultItemExcludes);runtime\**</DefaultItemExcludes>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="ModelContextProtocol.AspNetCore" Version="2.0.0" />
    <PackageReference Include="Radzen.Blazor" Version="8.0.0" />
    <PackageReference Include="DiffPlex" Version="1.9.0" />
    <PackageReference Include="Markdig" Version="0.41.3" />
    <PackageReference Include="Microsoft.Data.Sqlite" Version="10.0.8" />
    <!-- Patched native SQLite bundle: overrides the transitive lib.e_sqlite3 2.1.11
         (NU1903 / GHSA-2m69-gcr7-jv3q) that Microsoft.Data.Sqlite 10.0.8 pulls in. -->
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" Version="2.1.12" />
  </ItemGroup>

  <ItemGroup>
    <Content Update="config\appsettings.json" CopyToOutputDirectory="PreserveNewest" />
    <Content Update="config\appsettings.template.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\AIMonitor.Core\AIMonitor.Core.csproj" />
    <ProjectReference Include="..\AIMonitor.Logging\AIMonitor.Logging.csproj" />
    <ProjectReference Include="..\AIMonitor.Data\AIMonitor.Data.csproj" />
    <ProjectReference Include="..\AIMonitor.MSBuild\AIMonitor.MSBuild.csproj" />
    <ProjectReference Include="..\AIMonitor.Workflow\AIMonitor.Workflow.csproj" />
    <ProjectReference Include="..\AIMonitor.Indexing\AIMonitor.Indexing.csproj" />
    <ProjectReference Include="..\AIMonitor.McpServer\AIMonitor.McpServer.csproj" />
  </ItemGroup>

</Project>
