{
  "//": "Committed sample config for the CalculatorSample smoke run. The paths are relative on purpose: WatchedSolutionPath resolves against this file's own folder and RuntimeRoot against the repo root, so the file works in any checkout.",
  "Monitor": {
    "WatchedSolutionPath": "../../../samples/watched-solutions/CalculatorSample/CalculatorSample.slnx",
    "RuntimeRoot": "runtime/calculator-sample",
    "WinMergeCandidatePaths": []
  }
}
