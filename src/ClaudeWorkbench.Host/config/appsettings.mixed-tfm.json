{
  "//": "Committed sample config for the MixedTfmSample multi-project / multi-TFM overlay test bed (Blazor net10, WinForms net9, Console net8, Shared net8). Paths are relative on purpose: WatchedSolutionPath resolves against this file's own folder and RuntimeRoot against the repo root, so the file works in any checkout. Point the Host or MCP server at it with --config config/appsettings.mixed-tfm.json.",
  "Monitor": {
    "WatchedSolutionPath": "../../../samples/watched-solutions/MixedTfmSample/MixedTfmSample.slnx",
    "RuntimeRoot": "runtime/mixed-tfm"
  }
}
