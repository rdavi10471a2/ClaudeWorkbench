<SourceTab Snapshot="@snapshot"
           Filter="@filter"
           FilterChanged="@OnFilterChanged"
           SelectedPath="@selectedPath"
           IsRebuildingIndex="@rebuilding"
           ApplyFilter="@ApplyFilter"
           RebuildIndex="@RebuildAsync"
           SelectFile="@OnSelectFile" />
